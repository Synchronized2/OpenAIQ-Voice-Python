# 婚纱雏田 2.5D 角色包

此角色包由项目 `new_models/png` 中的婚纱立绘和表情参考图生成，供个人娱乐使用。

它使用 OpenAIQ 的 `openaiq-2.5d-avatar` 格式，不是 Live2D Cubism Editor 导出的 `.moc3`。当前支持呼吸、身体摇摆、鼠标视差、自动眨眼、朗读口型，以及聆听、思考、执行、朗读和错误状态表情。

如需重新生成表情层，请在项目根目录安装 Pillow 后执行：

```powershell
python scripts/build_wedding_hinata_avatar.py
```
